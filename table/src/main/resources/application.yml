spring:
  datasource:
    url: jdbc:mysql://122.51.240.129:3306/world
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      naming:
        physical-strategy: org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.qbt.test: DEBUG
server:
  port: 8080